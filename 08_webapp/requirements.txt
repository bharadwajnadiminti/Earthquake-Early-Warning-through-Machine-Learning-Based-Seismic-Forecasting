# Additional dependencies for stage 08 (forecast dashboard), on top of
# the root project's requirements.txt (pandas, numpy, scikit-learn,
# xgboost, joblib - all reused directly, not reimplemented).
flask==3.1.3
